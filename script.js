const header=document.querySelector('.header');addEventListener('scroll',()=>header.classList.toggle('scrolled',scrollY>40),{passive:true});
const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.14});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
document.querySelectorAll('details').forEach(item=>item.addEventListener('toggle',()=>{if(item.open)document.querySelectorAll('details').forEach(other=>{if(other!==item)other.open=false})}));
document.querySelector('#contact-form').addEventListener('submit',event=>{event.preventDefault();const data=new FormData(event.currentTarget);const nome=data.get('nome').trim(),empresa=data.get('empresa').trim(),telefone=data.get('telefone').trim(),servico=data.get('servico'),mensagem=data.get('mensagem').trim();const texto=`Olá, Brilho Santos! Meu nome é ${nome}${empresa?`, da empresa ${empresa}`:''}. Meu telefone é ${telefone}. Tenho interesse em ${servico}. ${mensagem}`;document.querySelector('#form-note').textContent='Mensagem pronta. Abrindo o WhatsApp…';window.open(`https://wa.me/5511986518754?text=${encodeURIComponent(texto)}`,'_blank','noopener')});document.querySelector('#year').textContent=new Date().getFullYear();
